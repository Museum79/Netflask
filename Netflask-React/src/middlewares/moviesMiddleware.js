

const moviesMiddleware = (store => (next) => (action) => {

switch (action.type) {

    // Requetes API à effectuer en fonction de l'action déclanchée lorsque on voudra utiliser notre API 




    default:
    next(action)
}




})